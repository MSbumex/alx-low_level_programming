#include "main.h"

/**
 * add - function
 * @a: integer a
 * @b: integer b
 * Description: to add two integers (a & b)and returns the result
 * Return: Result to add a and b
 */
int add(int a, int b)
{
	return (a + b);
}
